---
layout: "../layouts/Base.astro"
---

import Logo from "../components/Logo.astro";
import Title from "../components/Title.astro";
import CyclingTools from "../components/CyclingTools.astro";
import Byline from "../components/Byline.astro";
import GithubMention from "../components/GithubMention.astro";
import CharRepoDir from "../components/CharRepoDir.astro";
import Install from "../components/Install.astro";
import Command from "../components/Command.astro";
import CommandReference from "../components/CommandReference.astro";
import CommandDescription from "../components/CommandDescription.astro";
import Faq from "../components/Faq.astro";

<Logo stagger={1} />
<Title stagger={2}>Composable CLI for your meeting workflow</Title>
<CyclingTools stagger={3} />

<div class="stagger-4">

## Installation

Available for macOS (`x86` and `arm`).

Want other platforms? PRs welcome at <CharRepoDir name="apps/cli" />.

<Install command="npm i -g char" label="Or `npx char@latest --help`" />

## char record

<CommandDescription command="record" />

<Faq question="Why not just use default voice recorder app?">
It can record microphone(`--audio input`), system audio(`--audio output`) or both with `Echo Cancellation` applied(`--audio dual`).
</Faq>

## char transcribe

<CommandDescription command="transcribe" />

<Command command="char transcribe -i v.mp3 -p deepgram --api-key <KEY>" label="DEEPGRAM_API_KEY env needed. We support many other providers." />

<Command command="char transcribe -i v.mp3 -p whispercpp --model whisper-small-en" label="`char models download whisper-small-en` needed" />

<Faq question="Looking for realtime transcription, calendar syncing, etc?">
We have desktop app with all of it. Run `char desktop`.
</Faq>

## char models

<CommandDescription command="models" />

<Command command="char models download whisper-small-en" />

<Command command="char models list" label="try with `--format json` too" />


<Faq question="Is it Whisper.cpp?">
`Whisper.cpp` for all platforms, and additionally `Cactus` for `ARM`(e,g Apple Silicon).
</Faq>

## Reference

<CommandReference />

</div>

<div class="mt-36">
<Byline stagger={5}>Brought to you by <GithubMention name="@fastrepl/char" /></Byline>
</div>
